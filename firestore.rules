rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 家族専用ツール: 全読み書き許可 (本番では認証を追加推奨)
    match /mangaShelf/{docId} {
      allow read, write: if true;
    }
  }
}
