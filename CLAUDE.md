# 漫画棚アプリ - 開発ルール & エージェント運用フロー

## プロジェクト概要
- React Native (Expo Go) 製のアプリ
- Firebase Firestore でデータ共有・リアルタイム同期
- src/screens, src/components, src/hooks, src/lib の構成

## タスクの取得元
タスク(Todo)はNotionデータベースで管理する。このNotionデータベースは複数プロジェクトで共有しているため、
**「プロジェクト」列が「MangaShelf」または空欄のタスクのみを対象にする。**
「プロジェクト」列が他のアプリ名（splitpayなど）になっているタスクは絶対に処理しない（読み飛ばす）。

以下のプロパティを使う:

| プロパティ名 | 型 | 値の例 |
|---|---|---|
| 名前(タイトル) | Title | 「詳細画面に削除ボタンを追加」 |
| ステータス | Select | 未着手 / 進行中 / レビュー中 / テスト中 / デバッグ中 / 完了 / 要確認 |
| メモ | Text | 補足指示（任意、ユーザーが記入） |
| 実行結果 | Text | 処理完了後にClaudeが記入する要約（下記参照） |
| 完了日時 | Last edited time | 自動記録 |

このプロジェクトの自動処理は **Macのローカルセッション（`/loop`）でのみ**動作する。Xcode/iOSシミュレータ、VS Code連携などフル機能が使える前提で処理してよい。実行前にXcodeなどローカル環境が必要かを気にする必要はない。

## タスク結果の書き戻しルール

各タスクの処理が完了（または中断）するたびに、必ず以下の2箇所に結果を記録すること:

1. **「実行結果」プロパティ**（一覧で見える短い要約、100〜200字程度）
   例: 「詳細画面に削除ボタンを追加。review 1回で承認、test PASS。commit: a1b2c3d」

2. **タスクページの本文**（詳細ログ、トグルブロックで各エージェントの報告を残す）
   ```
   ## 実行ログ
   <details><summary>dev-agent</summary>
   （実装内容の報告をそのまま貼る）
   </details>
   <details><summary>review-agent</summary>
   （verdictと指摘内容）
   </details>
   <details><summary>test-agent</summary>
   （テスト結果）
   </details>
   <details><summary>debug-agent（発生した場合のみ）</summary>
   （原因と修正内容）
   </details>
   ```

- 差し戻し（review→dev、test失敗→debug）が発生した場合も、ラウンドごとの報告を全て本文に残す（省略しない）
- 「要確認」で中断した場合も、どこまで進んで何が原因で止まったかを本文に必ず書く（スマホで見た人が状況を把握できるようにする）

## 司令塔(このセッション)の動作フロー

`/loop` で定期実行される際、以下を行う:

1. **Notionのタスクデータベースを確認する**（`notion-query-data-sources` 等でステータス=「未着手」かつ プロジェクト=「MangaShelf」または空欄の行を取得）
2. 未着手タスクが無ければ何もせず終了する
3. 未着手タスクが1件以上あれば、**最も古いもの1件**を選び、ステータスを「進行中」に更新してから処理を開始する

### 処理パイプライン（1タスクにつき）

```
1. dev-agent にタスク内容を渡して実装させる
2. review-agent にレビューさせる
   - verdict が "request_changes" の場合 → 指摘内容を dev-agent に差し戻す（最大3回まで）
   - 3回差し戻しても解決しない場合 → Notionステータスを「要確認」にし、このタスクは中断して次へ
3. review承認後、Notionステータスを「テスト中」に更新
4. test-agent にテストを実行させる
   - PASSなら次へ
   - FAILなら debug-agent に引き継ぐ（Notionステータス「デバッグ中」）→ 修正後、test-agent に再テストさせる（最大3ラウンドまで）
5. テストPASS後:
   - git add / git commit（変更内容が分かる日本語コミットメッセージ）
   - git push
   - Notionステータスを「完了」に更新し、コミットハッシュをメモ欄に追記
```

### 全体ルール
- 各タスクは他のタスクと独立して処理する（同一ファイルを同時に触らないよう、1タスクずつ順番に処理する）
- 各エージェントとのやり取りはこのセッションのTodo/タスクリストで進捗を可視化する
- 破壊的な変更（DBスキーマ変更、依存パッケージの大規模更新など）を伴うタスクは、実行前に一度立ち止まり、Remote Control経由でユーザーに確認を取る
- APIキーやFirebase設定などの秘匿情報はコミット対象に含めない（.gitignore を確認する）

## Git運用
- commit: 意味のある単位で自動実行してよい
- push: テストPASS後、自動実行してよい
- force push は禁止
